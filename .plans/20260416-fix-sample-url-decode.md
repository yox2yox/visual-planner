# Sprint Contract

## 実装スコープ
- `fromUrlSafeBase64` に `=` パディング補完処理を追加し、パディングなしのURL-safe base64を正しくデコードできるようにする
- README.md のエンコード方法サンプルコードをURL-safe base64に修正する
- README.md のサンプルURLをURL-safe base64でエンコードした文字列に差し替える

## 検証基準
- [ ] サンプルURLをブラウザで開いてプランが正しく表示される
- [ ] パディング付き標準base64もデコードできる（後方互換性）
- [ ] パディングなしURL-safe base64もデコードできる
- [ ] 日本語を含むプランが正しくデコードされる
- [ ] 既存テストがすべてパスする
- [ ] typecheck がパスする

## 影響ファイル
- `plan-viewer/src/utils/decode.ts` — `fromUrlSafeBase64` にパディング補完を追加
- `plan-viewer/README.md` — エンコードサンプルとサンプルURLをURL-safe base64に修正
- `plan-viewer/src/utils/__tests__/decode.test.ts` — パディング付き/なし両方のテストケース追加

## リスク・制約
- 既存のURLが標準base64でエンコードされている可能性がある → パディング補完により後方互換性を維持

## 未解決の質問
- なし

## 除外事項（スコープ外）
- エンコード関数の追加（現在テストコード内のみ。プロダクションコードへのエクスポートは行わない）
- App.svelte の変更（デコードロジック自体が修正されるため不要）

## 実行モード判定（REQUIRED）
- モード: 逐次
- 判定根拠: decode.ts の修正 → README の修正 → テスト追加は密に依存しており、独立して分割できない

## 推奨事項
- パディング補完は `(4 - (str.length % 4)) % 4` 個の `=` を末尾に追加する標準的な方法で行う
- README のサンプルURLは、テストコード内の `encode` 関数と同じ方式（`+`→`-`, `/`→`_`, `=`除去）で再生成する
